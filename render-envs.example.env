# Copy these values into Render (Service → Environment)

MONGODB_URI=
MONGODB_DB_NAME=digiboard
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Set to your Render URL + /api/auth/google/callback
GOOGLE_REDIRECT_URI=

# Optional
PORT=3000
